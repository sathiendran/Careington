(function($, $snap) {
    var snap = {
        utilities : {
            browsers: {
                chrome: "53",
                firefox: "49"
            }
        }
    };

    $.extend($snap, snap);

})(jQuery,snap);