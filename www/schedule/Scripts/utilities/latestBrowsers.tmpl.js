(function($, $snap) {
    var snap = {
        utilities : {
            browsers: {
                chrome: "YY",
                firefox: "XX"
            }
        }
    };

    $.extend($snap, snap);

})(jQuery,snap);